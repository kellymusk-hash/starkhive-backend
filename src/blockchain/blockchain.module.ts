import { Module } from '@nestjs/common';
// import { BlockchainEventListener } from './provider/blockchain.service';

@Module({
// providers: [BlockchainEventListener],
})
export class BlockchainModule {}
